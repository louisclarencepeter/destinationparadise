# Privacy declaration worksheet

Prepared 5 September 2026; Nearby and live-policy evidence updated 7 September 2026 EAT. This is an engineering evidence worksheet; use [store-privacy-answers.md](store-privacy-answers.md) for the verified saved/published form answers and provider/account findings. It would be inaccurate to describe this app as collecting no data. Code paths in the evidence column are relative to the mobile package root; `../netlify` refers to the repository's website functions.

Nearby passed local validation and Android code `6` native functional QA; signed iOS build `5` inspection/upload also passed. Corrected Android code `7` passed artifact/device-coverage and bounded native QA and is published to the existing owner-only internal track, with zero devices lost. The revised policy is live and source-matching. [PUBLISH_STATUS.md](PUBLISH_STATUS.md) records exact evidence and the still-unverified iOS build `5` processing/group state; no public release is established.

## Observed data flows

Weather (`/api/weather`) and AI reply reports (`/api/planner-report`) use `https://destination-paradise-mobile.netlify.app`, alongside the [public privacy page](https://destination-paradise-mobile.netlify.app/mobile-privacy.html). The 7 September policy deployment preserved all three mobile functions and left the main website and environment settings unchanged. Canonical and immutable policy responses matched the revised source; weather returned 200, marine 200/unavailable, and report GET/OPTIONS returned 405/204 without email. Exact deployment and hash evidence is in [PUBLISH_STATUS.md](PUBLISH_STATUS.md). Successful report receipt remains unverified. Planner chat (`/api/planner`) and quotes (`/api/planner-send`) continue to use `https://yournexttriptoparadise.com`, as do ordinary Explore/detail photos and travel/support links.

| Feature | Data and destination | Storage and purpose | Source evidence |
| --- | --- | --- | --- |
| Saved places and preferences | Destination IDs; month, nights, party counts, budget category, pace, interests and style; stored through AsyncStorage. | Local preference storage. Settings resets this data. Saved IDs are not automatically sent as a list. Selected planning context is sent only when AI planning is used. Android introspection permits OS backup (`allowBackup:true`); actual device backup/restore was not tested. | `src/state/trip-store.tsx`, `src/state/preferences.ts`, `app/settings.tsx` |
| Nearby | Opt-in approximate foreground reading matched against prebundled destinations and related experiences. Android coarse access; iOS reduced accuracy by default. | Coordinates remain in memory only; only the enabled preference persists. One-shot refresh on new session/return to foreground while enabled, plus manual refresh and turn-off. No background tracking, current-location pin, tile centering or reverse geocoding. Coordinates, distances and current-location labels are not sent to backend or AI. Planning uses only a selected destination in the existing flow. | `src/features/nearby/location-service.ts`, `nearby-location-provider.tsx`, `nearby-content.tsx`, `nearby-model.ts`; validation and signed-artifact scope in [PUBLISH_STATUS.md](PUBLISH_STATUS.md). |
| AI planning | Trip context and recent user/assistant text, plus language, travel over HTTPS to `yournexttriptoparadise.com/api/planner` on Netlify, then `api.anthropic.com/v1/messages`. | Explicit consent before first request. Server forwards up to 16 messages / 6,000 total characters. Native app holds chat/contact form state in memory, not AsyncStorage. This does **not** establish zero server/provider retention. | `src/features/planner/planner-screen.tsx`, `src/features/planner/planning-context.ts`, `src/api/planner.ts`, `../netlify/functions/planner.mjs` |
| Quote request | Name, email, optional phone, trip context and reviewed transcript sent via HTTPS to `/api/planner-send`; Resend sends a team email and attempts a guest copy. | Separate consent and explicit Send action. Transcript accompanies contact identity in team email. Team mailbox and email provider records persist; actual retention schedule not verified. No automatic booking/payment action. | `src/api/planner.ts`, `../netlify/functions/planner-send.mjs`, `../netlify/functions/_shared.mjs` |
| AI asks for contact details | Existing website AI system prompt may ask for name/email/optional phone in chat. | If a user answers in chat, that contact text goes to Anthropic with subsequent requests. The separate contact form fields are not automatically included in chat requests. Do not imply contact data can only reach Resend. | `../netlify/functions/planner.mjs` system prompt; `src/api/planner.ts` |
| AI-content report | Exact selected assistant reply, reason, optional notes and affirmative consent sent through `/api/planner-report` and Resend to the configured planner team mailbox. No full chat or separate contact fields are accepted. | Optional user-reviewed submission for safety/quality review. Selected output/notes can contain personal details. Email report records persist under actual team/provider retention. Deployed endpoint rejects GET/invalid POST; no live report was sent, so successful production receipt remains a separate controlled check. | `src/features/planner/report-ai.tsx`, `src/api/report-ai.ts`, `../netlify/functions/planner-report.mjs` |
| Maps | The controlled map WebView directly requests `tile.openstreetmap.org/{z}/{x}/{y}.png`. OSMF is an independent controller; its retained logs include IP, client country inferred from IP, requested map tiles, time and app/browser details. | Interactive tiles, operations, security and usage analysis; normal device cache. No GPS access. Viewed tiles identify the viewed area; IP-derived country separately describes approximate device location. Exact provider log expiry was not established. | `src/components/map/map-document.ts`, `map-frame.tsx`; primary provider sources in `release/store-privacy-answers.md` |
| Photos | Automatic Nearby suggestion photos are bundled, with no image request based on location. Ordinary Explore/detail photos can load from public website image URLs and cache on device; servers/CDNs receive IP and ordinary request metadata. | Display destination inspiration; no user photo picker or uploads. Remote photographs are not the user's photo-library data. | `src/features/nearby/nearby-images.ts`, `src/features/explore/explore-ui.tsx`, `src/data/content.ts` |
| Current weather | App makes one request to Destination Paradise `/api/weather`; Netlify queries Apple WeatherKit for fixed Zanzibar coordinates `-6.222,39.224`. Upstream headers contain only server authorization and JSON Accept; client IP/contact/location headers are not forwarded. | Netlify receives ordinary client network metadata. Public readings use 60-second HTTP client caching and 600-second durable CDN caching with 60 seconds of stale revalidation. Source observation time is preserved. No marine fetch or alternative public API fallback exists. Live recovery checks passed after the policy deployment; see [PUBLISH_STATUS.md](PUBLISH_STATUS.md). | `src/api/weather.ts`, `../netlify/functions/weather.mjs`, `../netlify/functions/_weather_proxy.mjs` |
| Weather branding / attribution | Official Apple Weather PNG is bundled locally. A user can deliberately open the Apple legal-attribution/data-source page through an external link. | Displaying the logo does not make an Apple network request. Visiting the external legal page is a separate user action handled by the external browser/site. | `src/features/weather/weather-screen.tsx`, `assets/apple-weather-dark.png` |
| Security / diagnostics | Netlify functions use client IP for rate-limit buckets. Optional Sentry integration sends server failure context including endpoint, method and truncated upstream error bodies. | Rate-limit buckets persist in server-instance memory beyond a single request. Sentry uses `sendDefaultPii:false`, but upstream error bodies still require review. Provider access-log/tracing retention and production enablement are unknown. | `../netlify/functions/_shared.mjs`, `_sentry.mjs`, `planner.mjs`, `planner-send.mjs` |
| External links | `Linking.openURL` opens website booking/experience pages, map venue queries, events, privacy and source links. | Subsequent browsing happens outside the native app and follows the destination service's policies. The controlled Leaflet WebView is still part of this app's data-flow assessment. | Explore components, `app/settings.tsx` |

No native analytics, ad SDK, tracking prompt, push notification registration, account creation, camera, microphone, contacts access or photo upload was found in the inspected mobile source/dependency manifest. Nearby adds opt-in approximate foreground location as described above. This is a source/design finding, not a substitute for final binary/SDK and runtime review. Server Sentry and ordinary provider access logs must still be assessed.

## Apple draft mapping

| Data category | Proposed treatment and reason |
| --- | --- |
| Contact Info: Name, Email Address, Phone Number | Collected for optional quote functionality and potentially asked for in AI chat. Linked to the user in email/transcript. App Functionality; no ad-tracking purpose found. Phone is optional. |
| User Content: Other User Content | AI chat and travel preferences/draft. Conservative linked-to-user treatment where sent with contact details. App Functionality; assess Product Personalization for tailored suggestions. |
| User Content: Customer Support | New AI-content reports contain selected output, reason and optional notes. Declare the appropriate user-content/support category and App Functionality purpose. No account/contact identity is automatically attached, but text itself may identify someone; do not promise anonymity. |
| Diagnostics: Other Diagnostic Data / Performance Data | Conditional on actual retained server monitoring/traces and final native SDK behavior. Verify production Sentry and hosting logs before selecting categories/linkage. |
| Other Data Types / identifiers inferred from IP | Review retained IP/access metadata against Apple's taxonomy and provider use. No advertising/device identifier API was found; do not equate this with no network metadata. |
| Coarse Location; Product Interaction | Collected through OSM country and map-request records; linked conservatively, not tracking. App Functionality and Analytics. Nearby's separate on-device-only approximate reading adds no collected location category or purpose if QA confirms that no coordinates or derived current-location information leave the device. See the later worksheet's primary provider evidence. |
| Search History | Explore searches are local source filtering; no search-text transmission found. |
| Tracking | No cross-company advertising tracking purpose found. Confirm provider contracts/use before attesting. |

Apple excludes strictly on-device processing from collection and treats retention beyond servicing the request differently from instantaneous processing. Generic free text maps to user content; specifically requested contact fields need their contact categories. Apply the final provider practices, not just the absence of a mobile database. [Apple privacy definitions](https://developer.apple.com/app-store/app-privacy-details/)

## Google Play draft mapping

| Data type | Proposed treatment and purpose |
| --- | --- |
| Personal info: Name, Email address, Phone number | Collected; feature-optional overall, with name/email needed when the user elects to request a quote. App functionality and developer communications for quote follow-up. Do not mark ephemeral: email records persist. |
| Messages: Other in-app messages | Collected AI conversation and reviewed transcript. App functionality; optional AI feature. Do not mark ephemeral without verified provider retention. |
| App activity: Other user-generated content | Assess trip preferences and any report text that is not already covered as messages. App functionality / personalization, according to actual use. |
| App info and performance: Diagnostics / Other app performance data | Verify retained server error/tracing data and any final-binary SDK collection. |
| Device or other IDs | Verify actual retained network identifiers and provider practices; no installation/ad ID generated by mobile source. |
| Approximate location; App interactions | Collected and shared through OSM country/map-request records; automatic and non-ephemeral. App functionality, Analytics, Fraud prevention/security/compliance. Nearby's on-device-only approximate reading is excluded from collection; retain existing OSM treatment and verify no derived current-location transmission. |
| Sharing | Do not answer globally from source. Service-provider transfers and user-directed actions can have exceptions, but only if actual contracts and use qualify. Check Anthropic, Resend, hosting and OSM. Inspected WeatherKit code sends fixed public destination coordinates and server authorization, without forwarding client IP/contact/location headers. |
| Encryption in transit | Inspected production endpoints use HTTPS. Recheck release environment overrides; HTTPS is not end-to-end encryption. |
| Deletion | Local trip reset exists. Business policy offers contact-based deletion requests; verify an operational off-device request process and published URL before selecting a deletion commitment. No account creation exists, so account deletion is distinct. |

Google counts off-device transmission, including controlled WebViews. Its form still asks about ephemeral processing, while qualifying processor transfers may be excepted from “sharing.” Final form answers require the vendor relationships and actual retention. [Google Data safety definitions](https://support.google.com/googleplay/android-developer/answer/10787469)

## Facts the owner must resolve

1. Actual legal operator/contact and the applicable retention/deletion process for enquiries, team mailbox, Resend, provider logs, AI prompts, and later AI reports. Do not invent retention periods.
2. Anthropic account terms: processing regions, retention, any zero-retention arrangement, training/data-use terms and subprocessor obligations. Source alone proves neither zero retention nor a no-training promise.
3. Resend and team mailbox retention/access, optional Sentry enablement/traces, and Netlify logging behavior. `sendDefaultPii:false` does not prove no request-derived diagnostics.
4. Hosting access-log retention still needs provider/account confirmation; code caching applies to public weather readings, not log retention. WeatherKit activation and the dedicated proxy's live deployment are verified under the existing membership allowance. Service keys stay on the server, and no Open-Meteo purchase or new weather subscription was made. Final signed-build runtime checks remain separate from the public endpoint check.
5. Complete store-delivered runtime checks. Nearby iOS build `5` and corrected Android code `7` signed-artifact checks passed within the scope in [PUBLISH_STATUS.md](PUBLISH_STATUS.md); Expo Go is not their permission evidence.
6. Any business follow-up beyond the requested quote. No newsletter opt-in or advertising transmission was found in the native flow; marketing reuse would change the declarations.

The deployed 7 September `mobile-privacy.html` describes optional on-device Nearby location and bundled suggestion photos, alongside the existing OSM independent-controller, country-inference and operational/security/usage-analysis disclosures. Canonical and immutable bytes matched source; [PUBLISH_STATUS.md](PUBLISH_STATUS.md) records the deployment and SHA-256. App configuration and listing drafts use this URL; store declarations and app distribution remain separate.
