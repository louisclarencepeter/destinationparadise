# Store release preparation

Release records and review drafts prepared from the mobile source, verified store setup and Netlify services. Store app records now exist; the listing drafts and build records do not establish review approval, public availability, provider contracts or legal copyright ownership.

Earlier Nearby implementation validation passed 78 tests and all-platform exports; the current version correction now passes 79 tests, including generated iOS permission regression. The revised privacy policy remains live and source-matching.

Current **1.1.0** private release: iOS **build 6 is Complete and Testing** in the existing owner group; Android **code 8 is Active / Available to internal testers / Not reviewed** as internal release 5. All **79 tests**, typecheck and Android phone/tablet version/permission/manual-fallback checks passed. New physical-device installations and public release remain pending. See [PUBLISH_STATUS.md](PUBLISH_STATUS.md) for artifacts, provider readback and historical build 5 processing failure.

- [Current publishing status and remaining steps](PUBLISH_STATUS.md)
- [Listing copy and account fields](listing.en.json)
- [Review notes](review-notes.md)
- [Privacy data flows and declaration worksheet](privacy-data-flow.md)
- [Published mobile privacy source](mobile-privacy.html), verified live on the dedicated mobile site
- [Screenshot and asset plan](assets.md)

## Historical release evidence — 6 September 2026 EAT

The snapshots below predate Nearby. Current validation, privacy deployment and candidate status are recorded in [PUBLISH_STATUS.md](PUBLISH_STATUS.md).

Mobile tests passed **47/47**, backend tests **24/24**; typecheck, all-platform exports and a clean EAS archive install passed. The Router decoder fix is a licensed CommonJS adaptation of upstream 0.5.0. Corrected code `4`'s merged manifest includes `INTERNET`, `VIBRATE`, `ACCESS_NETWORK_STATE` and the app's signature-only internal receiver permission; see [actual artifact evidence](assets/android/qa/code4-runtime-manifest.json). Detailed evidence and remaining native QA are recorded in [VALIDATION.md](../VALIDATION.md). The original [source fingerprint manifest](build-source-sha256.json) is preserved; the later submission-only EAS configuration change is separately recorded in [submission-config-update.json](submission-config-update.json).

Dedicated mobile deployment `6a9c9097c7e4b26b67fadcd5` superseded the initial deployment at 21:59 UTC on 5 September 2026 (6 September EAT), publishing the clarified OSM privacy disclosure. The [canonical mobile privacy page](https://destination-paradise-mobile.netlify.app/mobile-privacy.html) returned HTTP 200 and matched source SHA-256 `e6c4606a95cfc0083111bf13fad61aac82769fcbcf9cdacca5a4447c108cdb18` (SHA-1 `bea6907ae99111038b5d5db391ed431930c6dc48`). Weather returned HTTP 200 with Apple observation `2026-09-05T21:59:16Z` (23.56 °C, humidity 0.86, `MostlyClear`); marine returned 200 with unavailable/null sea temperature; report GET returned 405 without email. All three function ZIPs and backend source hashes are unchanged, all seven runtime settings were preserved, and no environment or linked-site setting changed. The main website's published deployment remains `6a8dbf35cb01b33e235abc4b`. Store review/publication remains separate.

Earlier iOS Simulator build `e9a5dd2a-d500-4daa-a32e-8fea7de77472` is **FINISHED**; its artifact was downloaded and the standalone bundle `com.yournexttriptoparadise.mobile` installed on iPhone 17 Pro and iPad Pro 11-inch (M5). Phone Explore showed real OSM tiles and seven Zanzibar pins; iPad landscape showed the map/sidebar and Paje details. Both showed the live Apple reading rounded to 24 °C, `MostlyClear`, humidity 86%, sea unavailable and source time 20:51:08 UTC; iPad used two columns. Apple logo/legal attribution and phone Pause → Play → Pause were checked. Browser Weather matched the production values and Pause/Play worked.

iPad landscape-to-portrait rotation preserved correct weather, the two-column layout and bottom tabs without overflow. The compiled standalone `Info.plist` confirms version `1.0.0`, build `1`, minimum iOS `16.4`, phone/tablet families `[1,2]`, all four orientations, ATS arbitrary loads disabled, local networking enabled and non-exempt encryption `false`.

Corrected Android code `4` is published as internal release `2`, `1.0.0 (4) — weather rotation fix`, Active, Available to internal testers and Not reviewed. The fresh Testers tab confirms the one-user owner list only; other lists remain unchecked. The unreviewed track still uses its temporary package label, although the Destination Paradise Travel listing name is saved. Corrected code `4` finished and passed exact-AAB-derived standalone phone/tablet cold-launch and repeated-rotation QA, including the complete December card/CTA, live Apple Weather and Pause/Play; no app error lines were recorded. The exact AAB was accepted as `4 (1.0.0)`, min SDK 24 / target 36, and Save and publish succeeded. Play showed one nonblocking missing-deobfuscation-mapping warning and attached native debug symbols; owner opt-in/install remains unverified. Corrected signed iOS build `3` finished, passed static IPA checks and completed its EAS upload; Apple shows **Ready to Test** in the owner-only group with one **Invited** tester and automatic distribution off. Invitation acceptance/install is not established. App Store version build `3` selection is saved and was verified after navigation; final App Review has not been submitted. Physical-device, iOS store-signed runtime and remaining consent/service/recovery checks remain separate. Full IDs, hashes and limits are in [PUBLISH_STATUS.md](PUBLISH_STATUS.md). WeatherKit uses the existing membership allowance; no new weather subscription was made.

## Store records and submission setup

The user confirmed the existing **Louis Peter** publisher account. The copyright entry `2026 Destination Paradise` and named App Review contact Louis Peter (`louisclarencepeters@gmail.com`, `+255768779517`) are saved and visually verified. This records the store entries without inventing a different registered legal operator. The user-approved **Destination Paradise Travel** name is saved and verified in Apple after navigation and in Google's final saved listing. DSA details remain an owner decision.

| Store | Verified setup |
| --- | --- |
| Apple | App ID `6809042574`; listing name **Destination Paradise Travel**, saved and verified after navigation; SKU `destination-paradise-mobile`; primary language English (U.S.); bundle `com.yournexttriptoparadise.mobile`. The runtime app name remains Destination Paradise. |
| Google Play | App ID `4972417635244765220`; developer `7868106536966385994`; listing **Destination Paradise Travel**, final save verified; Send for review pending; free; English (U.S.); package `com.yournexttriptoparadise.mobile`. |
| iOS signing | Existing Louis Peter team `86ZX7JC345` and distribution certificate `Q4TN3C6SBG` were reused; provisioning profile `92C9TGV5GG` was created for the production build. These are identifiers, not stored private keys. |
| iOS EAS Submit | `submit.production.ios` targets Apple username `louisclarencepeters@gmail.com`, existing ASC app `6809042574` and team `86ZX7JC345`. Corrected build `3` submission `c48ecd45-1469-4f70-a552-67b58c6162cf` subsequently finished; root verified Apple processing and owner TestFlight invitation separately. |

Google production access is not available yet. This account requires a closed test with at least **12 testers continuously opted in for 14 days**, followed by an application for production access. An internal-release upload does not complete that requirement. [Google closed-testing requirements](https://support.google.com/googleplay/android-developer/answer/14151465?hl=en)

No production review approval or public store availability is established. The original EAS fingerprint reflects the configuration used for the build archive; `submit.production.ios` was added after the Android build and iOS production build submission. The later Weather rotation fix was archived and built as Android code `4` / iOS build `3`; its separate [source manifest](weather-layout-build-source-sha256.json) preserves the original build evidence. [EAS submission fields](https://docs.expo.dev/eas/json/#ios-specific-options-1)

## Concrete release gates

| Gate | Evidence at this audit | Work to close it |
| --- | --- | --- |
| AI reply reporting | `Report reply`, its reviewed consent-based modal and the endpoint are implemented and deployed. Live GET/invalid POST probes returned 405/400 without email. Provider-accepted success is covered by mocked tests. | Verify the final native interaction and controlled operational delivery, and maintain report review. Invalid-request probes and mocked tests do not establish production receipt. [Google policy](https://support.google.com/googleplay/android-developer/answer/13985936?hl=en-GB) |
| AI safety handling | The audited server prompt scopes advice to travel and prohibits invented availability. No dedicated app-level restricted-content filter was found. The new report endpoint gives the team material for review. | Verify effective provider/app safeguards with harmful and off-topic prompts, add controls where needed, and use reports to improve them. Record what the production AI provider handles versus what the app enforces. Do not claim an untested filter. |
| Mobile privacy coverage | The revised mobile policy is published at `https://destination-paradise-mobile.netlify.app/mobile-privacy.html`; canonical/immutable HTTP 200 responses matched source SHA-256. [Exact deployment evidence](PUBLISH_STATUS.md). App configuration and listing drafts use this URL. | Confirm the link in the final native candidate and use it in the store forms. Publishing the policy does not complete account-specific privacy declarations. |
| WeatherKit deployment | The production endpoint returned HTTP 200 with the original Apple observation time. Standalone iPhone/iPad Simulator apps and browser displayed matching production values; native/browser Pause/Play worked. | Complete release-device and offline/recovery checks, retain official Apple attribution and monitor the included allowance. No new weather purchase or public-provider fallback is part of this release. |
| Privacy declarations | Apple's nine-category privacy label is published. Google's ten-type Data Safety form was restored after the failed save, previewed and successfully saved. | Google saved changes await Send for review. Exact saved answers and source evidence are in [store-privacy-answers.md](store-privacy-answers.md); earlier save/access failures are resolved. |
| Final native QA | Nearby code `6` passed exact-AAB-derived Android functional QA. Code `7` passed artifact/device-coverage and bounded native upgrade checks with the same Hermes runtime; the phone emulator's location timeout recovered correctly. iOS build `5` passed signed-artifact inspection. [Current evidence](PUBLISH_STATUS.md). | Verify TestFlight and Play-delivered runtime, physical-device gestures, and remaining external-link/consent/reporting/offline checks. Keep email tests controlled. |
| Store assets | Apple has three iPhone 6.9-inch and three iPad 13-inch uploads. Google new name/copy/icon/feature and three images in each phone/seven-inch/large-tablet set have a verified final save. The seven-inch captures are authentic 1200×1920 native images. | Saved changes await Send for review; the final Review Save and its confirmation are verified. Saved assets do not establish a published store listing. |
| Publisher and store forms | Apple contact/copyright/Travel/13+/rights/URLs and zero pricing are saved; Apple privacy published. Google app-information setup is complete: the checklist and closed-test setup lock disappeared after Data Safety saved. Google saved changes await Send for review. | Resolve DSA choice and country availability; verify owner installation from the published internal track. The separate 12-tester/14-day closed test and production-access approval remain open. |

Existing AI consent is a positive finding: the planner explicitly names Destination Paradise and Anthropic, with an unchecked choice and a code guard before requests. Quote sharing has a separate review/consent step. Preserve both. Apple requires disclosure and affirmative permission before third-party AI receives personal information. [Apple review guidelines, 5.1.2](https://developer.apple.com/app-store/review/guidelines/#data-use-and-sharing)

## Build and operational notes

The dedicated mobile Netlify site is `destination-paradise-mobile` (site ID `f0fa9f3a-0de9-4e62-9809-d64031c415a2`). Weather (`/api/weather`), AI reply reports (`/api/planner-report`) and the public mobile privacy page use `https://destination-paradise-mobile.netlify.app`; live verification is recorded above. Planner chat (`/api/planner`) and quote sending (`/api/planner-send`) continue to use `https://yournexttriptoparadise.com`, alongside website marketing, travel-product, photo and support links.

The four task-created WeatherKit settings were removed from the old website site (DELETE 204, subsequent GET 404). Both sites' published deployment IDs remained unchanged. An unused draft could not be deleted (405) and was left unpublished; it is not a production deployment.

Current source config uses the user-approved version `1.1.0`, identifiers `com.yournexttriptoparadise.mobile`, tablet support and both orientations; current private binaries are iOS 1.1.0 (6) and Android 1.1.0 (8), with evidence in [PUBLISH_STATUS.md](PUBLISH_STATUS.md). SDK 57 documents iOS 16.4+, Android 7+, API target 36 and Xcode 26.4+. Confirm the generated candidate rather than treating Expo Go as the signed app. [Exact Expo SDK 57 reference](https://docs.expo.dev/versions/v57.0.0/)

Nearby signed-artifact checks confirm coarse-only Android access and iOS When In Use permission with reduced accuracy by default, with no background location permission. The code 7 compatibility correction made location hardware optional, retained in code 8. Code8 also links an unused Install Referrer binding through expo-application; no app invocation or automatic attribution flow was found. iOS 6 includes the required Motion purpose string but uses no motion data. Coordinates stay in memory and are not sent to backend, AI, maps or reverse geocoding; only the enabled preference is saved. Camera, microphone, contacts, photos and ad tracking are not requested. Exact artifact checks are recorded in [PUBLISH_STATUS.md](PUBLISH_STATUS.md); development tooling is not their permission evidence.

Nearby matches prebundled destinations and related experiences. Enabled users get a one-shot reading on a new session or return to foreground, plus manual refresh and turn-off. Unit/browser checks cover revoked/denied permission, disabled-state recovery and in-memory coordinates; native Android QA also passed successful coarse-location suggestions and relaunch. The map never adds a user pin or recenters on location. The revised `mobile-privacy.html` is deployed on the dedicated mobile site and its canonical/immutable bytes match source; exact deployment evidence is in [PUBLISH_STATUS.md](PUBLISH_STATUS.md). No location backend or provider change was required. Store data-collection answers remain unchanged only while processing stays exclusively on device; retain the existing OSM-derived approximate-location declarations.

OSM tiles are permitted for ordinary interactive viewing with attribution, app identification and normal caching. Source contains all three mechanisms and no bulk/offline download feature; verify actual release WebView requests and caching. Public tiles have no availability guarantee, so maintain the list fallback and a provider migration plan. [OSMF tile policy](https://operations.osmfoundation.org/policies/tiles/)

## Facts that still require owner/account evidence

- Whether to use existing trader details under review or different business details. The exact store name Destination Paradise Travel is approved and saved and verified in both stores. The saved copyright/contact fields are already verified. The public page does not invent a registered company name or street address.
- Reachability and ownership of the team/privacy mailbox and the operational process for handling reports and privacy requests.
- Provider contracts, processing regions, actual retention and any zero-retention arrangements. The public page uses purpose-based retention criteria already reflected in the website policy and does not promise fixed deletion deadlines or a no-training arrangement.
- Apple country availability and the separate Destination Paradise closed-test list. Apple zero pricing/13+ and Google IARC/18+ audience answers are saved; Google's ten-type Data Safety answers are now saved and verified after access recovery; Send for review remains pending. Apple WeatherKit access is verified under the existing membership allowance; keep credentials server-side and monitor that allowance.

The published mobile policy has no draft banner or unfilled fields. These internal questions still matter for account/store declarations; they should not be answered by assumption. Use [PUBLISH_STATUS.md](PUBLISH_STATUS.md) for subsequent build and account changes. Open gates here are not a claim that a later release change is still blocked.

The published privacy page describes the deployed WeatherKit flow. WeatherKit does not supply sea-surface temperature, so the app and store assets must show that field as unavailable or omit it rather than reuse an old marine reading.

Weather implementation evidence: the mobile app makes one forecast request to `/api/weather`, uses the source `asOf` time, and makes no marine request. The server forwards only fixed coordinates and its own authorization; it does not forward incoming client headers. Successful public weather responses use a 60-second HTTP client cache and a 600-second durable CDN cache with 60 seconds of stale revalidation. The official Apple Weather PNG is bundled, so displaying it causes no direct app-to-Apple asset request. Live production returned observation `2026-09-05T20:51:08Z`, which was also observed in the standalone iOS Simulator app.
