# Optional public backend override. Apple WeatherKit signing keys stay on Netlify.
# Default native endpoint is the dedicated mobile backend; browser preview uses /api/weather.
# EXPO_PUBLIC_WEATHER_FORECAST_URL=https://destination-paradise-mobile.netlify.app/api/weather
