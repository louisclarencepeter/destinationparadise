import Placeholder from '../components/Placeholder.jsx';
export default function Excursions() {
  return <Placeholder eyebrow="Roaming Retreats" title="Excursions" lead="A dedicated excursions browser will live here. The homepage already shows a featured grid sourced from the same data — we can promote that into a full page once a design is provided." />;
}
