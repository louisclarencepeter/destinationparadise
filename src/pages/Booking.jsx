import Placeholder from '../components/Placeholder.jsx';
export default function Booking() {
  return <Placeholder eyebrow="Plan your trip" title="Booking request" lead="A full booking form will replace this. For now, the homepage hero search and AI planner cover the same ground — and the WhatsApp button gets you to the team directly." />;
}
