node_modules
dist
dev-dist
# Ignore all env files (secrets) except the intentionally-shared, non-secret dev config.
.env
.env.*
!.env.development
.netlify
.DS_Store
*.log
.instagram-story-state.json
.instagram-story-log.jsonl
.threads-auto-commenter-state.json

# Docs-side asset/archive snapshots
docs/

# Local Claude Code tooling (preview launch config, local settings)
.claude/

# Local editor workspace settings
.vscode/

# Local scratch output (browser session logs, generated assets, temp files)
.playwright-cli/
output/
tmp/

# Supabase CLI link state (machine-local)
supabase/.temp/
